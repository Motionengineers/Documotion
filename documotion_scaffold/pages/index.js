export default function Home() {
  return (
    <main style={{fontFamily: 'system-ui, sans-serif', padding: 40}}>
      <h1>Documotion — Demo (no APIs)</h1>
      <p>This is a minimal scaffold. Connect a database and add env values to enable full features.</p>
    </main>
  )
}
